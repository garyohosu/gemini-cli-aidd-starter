# QandA.md

このファイルは、SPEC.md に基づく設計書作成中に発生した不明点・保留事項を記録します。  
設計を進める前に、これらの項目を確認・判断してください。

---

## Q1. 実装言語・フレームワークは何を使うか

**なぜ不明か:**  
SPEC.md には実装言語の指定がない。Windows 11 デスクトップアプリであることのみ明記されている。

**考えられる選択肢:**
- Python + tkinter（シンプル・学習コスト低）
- Python + PyQt / PySide（リッチUI）
- C# + WinForms（Windows 標準的）
- C# + WPF（モダンなUI）
- Electron（Web技術ベース）

**この判断が影響する範囲:**  
CLASS.md の実装詳細、UI.md のコンポーネント記述、TESTCASE.md のテスト手法すべてに影響する。

**現在の対応:** 設計書は言語非依存で記述。実装フェーズ前に確定が必要。  
**判断待ち / 暫定対応:** 判断待ち

---

## Q2. 時刻監視の間隔は 1 秒か 5 秒か

**なぜ不明か:**  
SPEC.md 7-1 節に「1秒ごと、または5秒ごとに現在時刻を確認する」と記載されており、どちらにするか確定していない。

**選択肢ごとの影響:**
- 1 秒間隔: CPU 使用率がやや上がるが、通知の遅延が最大 1 秒
- 5 秒間隔: 軽量だが、通知が最大 5 秒遅れる可能性がある

**現在の対応:** 設計書では「定期チェック」と表現し、間隔は実装パラメータとして保留。  
**判断待ち / 暫定対応:** 判断待ち

---

## Q3. 同じ分に複数のアラームが重なった場合の挙動はどうするか

**なぜ不明か:**  
SPEC.md には複数アラームの同時発火に関する記述がない。

**考えられる挙動:**
- 複数の通知ウィンドウを同時に表示する
- 先に見つかったものだけ表示し、残りは無視する
- キューに入れて順番に表示する

**現在の対応:** 設計書では未定事項として注記。  
**判断待ち / 暫定対応:** 判断待ち

---

## Q4. 通知ウィンドウが表示中に別のアラームが発火した場合の挙動はどうするか

**なぜ不明か:**  
Q3 と関連するが、「前の通知を閉じていない状態」で次のアラームが発火した場合の挙動が未定義。

**考えられる挙動:**
- 新しい通知ウィンドウを追加で開く（複数ウィンドウ）
- 既存の通知ウィンドウにメッセージを追加する
- 既存の通知を閉じてから新しいものを開く

**現在の対応:** 設計書では未定事項として注記。  
**判断待ち / 暫定対応:** 判断待ち

---

## Q5. JSON ファイルの保存パスはどこか

**なぜ不明か:**  
SPEC.md 8 節に「JSON ファイル」での保存と記載されているが、保存先パスの指定がない。

**考えられる選択肢:**
- 実行ファイルと同じディレクトリ（`alarms.json`）
- ユーザーのホームディレクトリ（`~/alarms.json`）
- AppData 配下（`%APPDATA%\QuietAlarm\alarms.json`）

**この判断が影響する範囲:**  
インストール方法・複数ユーザー対応・バックアップ手順に影響する。

**現在の対応:** 設計書ではパスを `[保存パス未確定]` と明記。  
**判断待ち / 暫定対応:** 判断待ち

---

## Q6. Ver 1.0 において通知ウィンドウは自動で閉じるか

**なぜ不明か:**  
SPEC.md 6-4 節に「自動で消さないか、または数十秒後に自動で閉じる設定も可」「最初は閉じるまで残るでいいと思う」と記載されているが、Ver 1.0 の確定仕様として「自動クローズなし」で確定してよいか判断が必要。

**現在の対応:** 設計書では「Ver 1.0 は手動クローズのみ」と仮記述し、要確認フラグを付記。  
**判断待ち / 暫定対応:** 暫定対応（手動クローズのみ）。確定後に修正。

---

## Q7. 起動前に過ぎた単発アラームの「完了」扱いはどうするか

**なぜ不明か:**  
SPEC.md 7-4 節に「起動前に過ぎた通知は基本的に出さない」と記載されている。しかし、その際に `done` フラグを自動で `true` に更新するかどうかが不明。

**パターンA:** 起動時に過去の未完了単発アラームを検出し、`done = true` に自動更新する  
→ 次回起動時も「完了」扱いとなる

**パターンB:** 起動時には何もせず、次の起動時も「まだ時刻が過ぎているか」を判定して通知しない  
→ `done` フラグは通知後のみ `true` にする

**この判断が影響する範囲:**  
SEQUENCE.md の起動シーケンスと TESTCASE.md の起動時テストケースに影響する。

**現在の対応:** 設計書では「起動時に時刻チェックを行い、過去の未完了単発アラームは通知しない（`done` は変更しない）」と暫定記述。  
**判断待ち / 暫定対応:** 暫定対応。確定後に修正。

---

## Q8. JSON ファイルが存在しない、または破損していた場合の挙動はどうするか

**なぜ不明か:**  
SPEC.md にエラー処理の記述がない。初回起動時やファイル破損時の挙動が未定義。

**考えられる挙動:**
- 新規ファイルとして空のデータで起動する
- エラーメッセージを表示して終了する
- バックアップがあれば復元を試みる

**現在の対応:** 設計書では「ファイルが存在しない場合は空データで起動、破損の場合はエラーメッセージを表示して空データで起動」と暫定記述。  
**判断待ち / 暫定対応:** 暫定対応。確定後に修正。

---

## Q9. 毎日アラームの有効時間帯の制限はあるか

**なぜ不明か:**  
毎日アラームの時刻として `00:00`〜`23:59` のすべてを指定可能と考えられるが、深夜帯（例: `00:00`〜`05:00`）など特殊な時刻への制限があるかどうかが未明示。

**現在の対応:** 設計書では制限なし（全時間帯対応）と暫定記述。  
**判断待ち / 暫定対応:** 暫定対応（制限なし）。

---

## Q10. 編集中のアラームが発火時刻を過ぎた場合の扱いはどうするか

**なぜ不明か:**  
ユーザーが単発アラームを編集中（ダイアログ表示中）に、その通知時刻を過ぎた場合の挙動が未定義。

**現在の対応:** 設計書では「編集ダイアログを閉じた時点で通知判定を行う。すでに時刻を過ぎていた場合は通知しない」と暫定記述。  
**判断待ち / 暫定対応:** 暫定対応。確定後に修正。

---

*最終更新: 設計書初版作成時*
