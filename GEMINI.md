# GEMINI.md

## このプロジェクトでの役割
あなたは、このリポジトリで AI駆動開発を進める実務担当である。  
作業は必ず **計画 → 実装 → レビュー** の順で進める。

## 重要ルール
- いきなり大規模実装を始めない
- まず既存ファイルを確認する
- 既存の `SPEC.md` `QandA.md` `USECASE.md` `SEQUENCE.md` `CLASS.md` `UI.md` `TESTCASE.md` を読む
- 不明点は勝手に決めきらず `QandA.md` に残す
- 変更は小さく区切る
- 実装後は必ず自己レビューまたはレビュー用コマンドを使う
- 重要な設計判断は Markdown に残す
- ユーザーに見せる説明は簡潔にする

## 作業モードの使い分け

### Plan Mode を使う条件
以下に当てはまる場合は、まず Plan Mode で考える。
- 新機能追加
- 既存構造に影響する変更
- `SPEC.md` から設計書を起こすとき
- エラー原因がまだ不明なとき
- 複数ファイルへまたがる修正

### 通常モードで進めてよい条件
- 小さな文言修正
- 明確な単一バグ修正
- テスト追加のみ
- レビュー結果を受けた軽微な修正

## 標準フロー

### 1. 仕様確認
- `SPEC.md` を読む
- 後工程へ影響する曖昧点があれば `QandA.md` に追記する
- 実装前に優先順位を整理する

### 2. 設計
必要なら以下を順番に生成する。
- `USECASE.md`
- `SEQUENCE.md`
- `CLASS.md`
- `UI.md`
- `TESTCASE.md`

### 3. 実装
- 設計書を読んでから進める
- 変更対象を絞る
- テストが必要なら追加する

### 4. レビュー
以下を優先して確認する。
- 前フェーズとの矛盾
- 必須項目の欠落
- Mermaid 構文エラー
- 例外状態の不足
- テスト観点不足

## Mermaid 優先ルール
- `USECASE.md` は `flowchart`
- `SEQUENCE.md` は `sequenceDiagram`
- `CLASS.md` は `classDiagram`
- `UI.md` は画面遷移があるなら Mermaid を含める
- ASCII 図のみで代用しない

## 出力形式
各作業の最後に次を短く出力する。
- 実施内容
- 変更ファイル
- 残課題
- 次の推奨アクション

## start-skill を優先してよい曖昧依頼
ユーザーが次のように言ったときは、まず親スキルの流れで現在地を判断する。
- 進めて
- 続けて
- 次をやって
- レビューして
- 仕様から進めて
- 設計書を起こして

## spec-to-design を使う条件
- `SPEC.md` が存在する
- まだ設計書群が十分でない
- 仕様から設計を段階的に起こしたい

## QandA.md に残すべき例
- 対象ユーザーが未定
- 認証方式が未定
- エラー時のリトライ仕様が未定
- API 制約が不明
- 設計方針が複数あり影響が大きい

## レビュー時の判定
- `APPROVED`
- `REQUEST_CHANGES`

レビューでは重箱の隅より、重大な破綻を優先すること。
細かい美しさにこだわりすぎて前へ進めなくなるのは禁止。
